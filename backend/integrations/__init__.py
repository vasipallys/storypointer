"""Integration catalog: a registry of the external tools the L1 platform can
connect to. Live adapters exist for a few (Jira, document import via AI); the
rest are catalogued with a connection status so the framework is extensible."""
